import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
import { load } from "npm:cheerio@1.0.0";
const URL="https://hokej.si/tekmovanja/"; const UA="Mozilla/5.0 (compatible; VCeljuSeNicNeDogaja/1.0; +https://vceljusenicnedogaja.si)";
const clean=(s:any)=>String(s??"").replace(/\s+/g," ").trim();
function tzIso(y:number,m:number,d:number,h=0,min=0){const wall=Date.UTC(y,m-1,d,h,min);const off=(ts:number)=>{const p=new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Ljubljana",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hourCycle:"h23"}).formatToParts(new Date(ts));const v:any=Object.fromEntries(p.map(x=>[x.type,x.value]));return Date.UTC(+v.year,+v.month-1,+v.day,+v.hour,+v.minute,+v.second)-ts};let u=wall-off(wall);u=wall-off(u);return new Date(u).toISOString()}
Deno.serve(async req=>{if(req.method!=="POST")return new Response("Method not allowed",{status:405});const sb=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,{auth:{persistSession:false,autoRefreshToken:false}});try{const {data:src,error:se}=await sb.from("sources").select("id").eq("key","hzs").single();if(se||!src)throw new Error(se?.message||"source missing");const r=await fetch(URL,{headers:{"user-agent":UA,accept:"text/html","accept-language":"sl-SI,sl;q=0.9"},redirect:"follow"});if(!r.ok)throw new Error(`HTTP ${r.status}`);const html=await r.text(),$=load(html),rows:any[]=[];const now=new Date().toISOString();$(".resulttab").each((_i,e)=>{const q=$(e),info=clean(q.find(".g-info").text()),teams=q.find("td.team").map((_j,t)=>clean($(t).text())).get().filter(Boolean);if(teams.length<2)return;const venue=info.match(/Dvorana\s+Celje|Drsališče[^]{0,80}?Celje/i)?.[0]||"";if(!venue)return;if(!/^HK\s+LedX\s+Celje$/i.test(teams[0]))return;const dm=info.match(/(\d{1,2})\.(\d{1,2})\.(20\d{2})/);if(!dm)return;const tm=info.match(/(?:\d{1,2}\.\d{1,2}\.20\d{2})\s+(\d{1,2}):(\d{2})/);const start=tzIso(+dm[3],+dm[2],+dm[1],tm?+tm[1]:0,tm?+tm[2]:0);if(Date.parse(start)<Date.now()-3600000)return;const title=`${teams[0]} – ${teams[1]}`;const id=`${dm[3]}-${dm[2]}-${dm[1]}-${teams.join('-')}`.toLowerCase().normalize('NFKD').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,140);rows.push({source_id:src.id,source_event_id:id,title,slug:`hzs-${id}`,start_at:start,end_at:null,all_day:!tm,venue,city:"Celje",category:"Šport",event_type:"single",is_free:null,source_url:URL,status:"published",location_status:"in_area",raw:{direct_source:"hzs",sport:"hokej",teams},last_seen_at:now,updated_at:now})});if(rows.length){const {error}=await sb.from("events").upsert(rows,{onConflict:"source_id,source_event_id"});if(error)throw error}await sb.from("sources").update({last_synced_at:now,updated_at:now}).eq("id",src.id);await sb.rpc("promote_direct_source_duplicates");return Response.json({ok:true,imported:rows.length,titles:rows.map(x=>x.title)});}catch(e){return Response.json({ok:false,error:e instanceof Error?e.message:String(e)},{status:502})}});